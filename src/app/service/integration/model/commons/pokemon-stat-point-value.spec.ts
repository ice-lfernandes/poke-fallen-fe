import { PokemonStatPointValue } from './pokemon-stat-point-value';

describe('PokemonStatPointValue', () => {
  it('should create an instance', () => {
    expect(new PokemonStatPointValue()).toBeTruthy();
  });
});
