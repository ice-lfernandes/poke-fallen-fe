import { TransferIntentRequest } from './transfer-intent-request';

describe('TransferIntentRequest', () => {
  it('should create an instance', () => {
    expect(new TransferIntentRequest()).toBeTruthy();
  });
});
