import { PlayerUpdateBasicRequest } from './player-update-basic-request';

describe('PlayerUpdateBasicRequest', () => {
  it('should create an instance', () => {
    expect(new PlayerUpdateBasicRequest()).toBeTruthy();
  });
});
