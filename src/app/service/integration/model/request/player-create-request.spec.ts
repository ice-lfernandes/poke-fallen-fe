import { PlayerCreateRequest } from './player-create-request';

describe('PlayerCreateRequest', () => {
  it('should create an instance', () => {
    expect(new PlayerCreateRequest()).toBeTruthy();
  });
});
